/**
 * The status bar module (spec section 7.15). `StatusBar` is the region element; `StatusItem` is
 * exported because the degraded banner and the console footer both want the same small clickable
 * mono segment.
 */
export { StatusBar } from './StatusBar';
export { StatusItem } from './StatusItem';
export type { StatusItemProps } from './StatusItem';
